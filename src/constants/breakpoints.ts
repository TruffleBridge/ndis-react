/** Sidebar switches to hamburger drawer at or below this width. */
export const MOBILE_NAV_MAX_WIDTH = 475;

export const MOBILE_NAV_QUERY = `(max-width:${MOBILE_NAV_MAX_WIDTH}px)`;

export const ABOVE_MOBILE_NAV_QUERY = `(min-width:${MOBILE_NAV_MAX_WIDTH + 1}px)`;
